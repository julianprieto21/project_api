export type AppType = {
  isLogin: boolean;
  setIsLogin: (isLogin: boolean) => void;
  isAdmin: boolean;
  setIsAdmin: (isAdmin: boolean) => void;
  isUser: boolean;
  setIsUser: (isUser: boolean) => void;
};
